DROP TABLE EMPREGADOS;
-- DROP SEQUENCE EMPREGADOS_SEQ;

CREATE SEQUENCE EMPREGADOS_SEQ;

CREATE TABLE EMPREGADOS
(NUMERO       NUMERIC(5,0)
   CONSTRAINT EMPREGADOS_NUMERO_PK
   PRIMARY KEY,
 NOME         VARCHAR(50),
 NUMDEP		  VARCHAR(40),	
 SALARIO      NUMERIC(11,0));

INSERT INTO EMPREGADOS
VALUES (NEXTVAL('EMPREGADOS_SEQ'), 'CLAUDIA REGINA', 0, 1.200);

INSERT INTO EMPREGADOS
VALUES (NEXTVAL('EMPREGADOS_SEQ'), 'IARA CRISTINA', 1, 3.500);

INSERT INTO EMPREGADOS
VALUES (NEXTVAL('EMPREGADOS_SEQ'), 'MATHEUS SANTOS', 2, 1.150);

SELECT * FROM EMPREGADOS;


DROP TABLE DEPARTAMENTOS;

CREATE TABLE DEPARTAMENTOS
(NUMDEP      NUMERIC(5,0)
   
   PRIMARY KEY,
 NOME         VARCHAR(50));

INSERT INTO DEPARTAMENTOS
VALUES (0, 'VAZIO');

INSERT INTO DEPARTAMENTOS
VALUES (1, 'DESENVOLVIMENTO');

INSERT INTO DEPARTAMENTOS
VALUES (2, 'CONTABILIDADE');

SELECT * FROM DEPARTAMENTOS;